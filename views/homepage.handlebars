{{#each articles as |article| }}
<div class="row align-center">
  <div class="">
    <h2>{{article.title}}</h2>
  </div>
  <div class="">
    <h4>{{article.author}}</h4>
  </div>
  <div class="">
    <p class="">{{article.body}}</p>
  </div>
</div>
{{/each}}